mutation($name:String, $email: String, $imagePath: String, $oauthProvider: String){
  createUser(input:{
    name:$name,
    email:$email,
    imagePath:$imagePath,
    oauthProvider:$oauthProvider
  }){
    id
  }
}